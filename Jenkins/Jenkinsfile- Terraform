pipeline {
    agent any

    environment {
        AWS_REGION = "me-south-1"
    }

    stages {

        stage('Checkout') {
            steps {
                git branch: 'main',
                    url: ''https://github.com/engrsaqi14-sketch/jenkin_testing.git''
            }
        }

        stage('Terraform Init') {
            steps {
                dir('terraform') {
                    sh 'terraform init'
                }
            }
        }

        stage('Terraform Validate') {
            steps {
                dir('terraform') {
                    sh 'terraform validate'
                }
            }
        }

        stage('Terraform Plan') {
            steps {
                dir('terraform') {
                    sh 'terraform plan -var-file=dev.tfvars'
                }
            }
        }

        stage('Terraform Apply') {
            steps {
                dir('terraform') {
                    sh 'terraform apply -var-file=dev.tfvars -auto-approve'
                }
            }
        }
    }

    post {
        success {
            echo "Terraform infrastructure deployed successfully using dev.tfvars"
        }
        failure {
            echo "Terraform pipeline failed. Immediate review required."
        }
    }
}
